# 사용자 작업 지침

이 지침은 이 저장소 전체에 적용한다. 이후 작업에서도 아래 수정 승인 절차와 주석 양식을 따른다.

## Item 폴더 외 스크립트 수정은 사전 승인 필수

- 사용자 요청 범위 내에서 다음 Item 폴더의 스크립트는 별도 사전 승인 없이 수정할 수 있다.
  - `TeamProject_MOU/Source/TeamProject_MOU/Public/Item/`
  - `TeamProject_MOU/Source/TeamProject_MOU/Private/Item/`
- 위 폴더 밖의 스크립트를 수정해야 한다면, 실제 수정 전에 반드시 사용자에게 다음 내용을 설명하고 검토를 요청한다.
  1. 수정할 파일의 경로와 대상 함수 또는 코드
  2. 수정이 필요한 이유와 수정하지 않았을 때의 영향
  3. 제안하는 변경 내용 및 기존 기능에 미치는 영향
  4. Item 폴더 안에서 해결할 수 있는 대안이 있다면 그 대안과 제약
  5. 현재 스크립트의 관련 코드와 수정 제안 코드를 코드 블록 또는 diff로 제시한다. 파일명과 함수명을 표시하고, 새로 추가하는 코드는 추가 위치를 명시한다. 전체 파일 대신 변경 판단에 필요한 문맥을 포함한다.
  6. 제시한 코드가 현재 어떤 역할을 하는지, 어느 부분 때문에 수정이 필요한지, 제안 코드가 어떻게 문제를 해결하는지 한국어로 설명한다. 선언·정의·입력 연결 등 여러 파일이 함께 동작하면 그 호출 흐름과 파일별 변경 필요성도 설명한다.
- 승인 요청에서 수정 이유만 요약하고 스크립트 내용을 생략하지 않는다. 제안 코드는 검토용으로 먼저 보여주며, 승인 전에 실제 파일에 적용하지 않는다.
- 사용자의 명시적인 승인이 나온 뒤에만 승인받은 범위의 수정을 수행한다. 일반적인 기능 구현 요청이나 빌드 오류 해결 필요성을 이 승인으로 간주하지 않는다.
- 승인 대기 중에는 대상 스크립트를 수정하지 않는다. 읽기 전용 조사와 승인에 의존하지 않는 작업은 진행할 수 있다.
- 승인 범위를 넘어서는 파일이나 변경이 추가로 필요하면 다시 설명하고 승인을 받는다.
- 스크립트의 신규 생성, 삭제, 이동, 이름 변경도 동일한 승인 절차를 적용한다.
- 파일명에 Item이 포함되어 있어도 위 Item 폴더 밖이면 승인 대상이다. 예를 들어 `Public/Base/ItemBase.h`, `Private/Base/ItemBase.cpp`, `Public/Player/MainCharacter.h`, `Private/Player/MainCharacter.cpp`는 사전 승인 없이 수정하지 않는다.
- 기존 사용자의 변경은 보존한다.

## 함수 CodeName 주석 양식

사용자가 제공한 `C:/Users/111/Desktop/MOU_주석양식.txt`의 규칙을 따른다. 아래에 해당 규칙을 보관한다.

- 형식: `[접두어-3자리숫자]`. 예: `[MAP-001]`, `[CONSUME-000]`, `[POTION-003]`, `[SPRAY-000]`.
- CodeName은 함수에만 붙인다. 변수에는 CodeName 없이 일반 설명 주석을 작성한다.
- 접두어는 기능 또는 도메인을 나타내는 대문자 약어를 사용한다. 예: MAP, FOG, ITEM, WEAPON, TASER, CONSUME, POTION, SPAWNER, SPRAY.
- 번호는 접두어별 일련번호로 000 또는 001부터 시작한다. 기존 번호와 충돌하지 않도록 확인한다.
- `.h` 함수 선언 위와 `.cpp` 함수 정의 위에 동일한 CodeName을 붙여 검색으로 둘 다 찾을 수 있게 한다.
- 주석에는 함수의 역할이나 처리 흐름을 한국어로 설명한다.
- 기존 코드에 주석을 적용하는 경우에도 위 수정 승인 범위를 준수한다. 주석만 바꾸는 작업도 Item 폴더 밖의 스크립트라면 사전 승인을 받는다.

예시:

```cpp
// [CONSUME-000] 좌클릭: 잔여 횟수 체크 → 서버 차감 → ApplyEffect → 소진 시 Destroy
virtual void OnUse_Implementation() override;
```

동일 함수의 `.cpp` 정의 위에도 `[CONSUME-000]`을 사용한다.
